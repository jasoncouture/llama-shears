# LlamaShears.Core.Abstractions.PromptContext

## Types

- [PromptContextMemory](PromptContextMemory.md) — One memory hit surfaced to the per-turn prompt-context template (IPromptContextProvider). The agent reads the body from disk via the read-file tool when it actually wants the content; the template only sees the summary and score.

