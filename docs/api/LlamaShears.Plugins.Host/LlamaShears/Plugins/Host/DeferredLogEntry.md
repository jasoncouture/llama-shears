# LlamaShears.Plugins.Host.DeferredLogEntry

Assembly: `LlamaShears.Plugins.Host`

One captured log call. [DeferredLogEntry](DeferredLogEntry.md).`Format` follows the .NET
`ILogger` message-template convention; [DeferredLogEntry](DeferredLogEntry.md).`Data`
carries the corresponding positional arguments.

## Methods

### `DeferredLogEntry`(LogLevel Level, string Format, Exception Exception, ImmutableArray<object> Data)

One captured log call. [DeferredLogEntry](DeferredLogEntry.md).`Format` follows the .NET
`ILogger` message-template convention; [DeferredLogEntry](DeferredLogEntry.md).`Data`
carries the corresponding positional arguments.

