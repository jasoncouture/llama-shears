# LlamaShears.Core.Abstractions.Events.Event

Assembly: `LlamaShears.Core.Abstractions`

Constants for the framework's well-known event sources and event
types. Anchors the strings used in [EventType](EventType.md).`Component`
and [EventType](EventType.md).`EventName` in one place so subscribers
reference symbols instead of magic strings.

