# LlamaShears.Core.Abstractions.Events.Event.Sources

Assembly: `LlamaShears.Core.Abstractions`

Canonical [EventType](../EventType.md).`Component` values.

## Fields

### `Agent`

Events emitted by an agent.

### `Channel`

Events emitted by chat channels.

### `Command`

Commands the runtime issues to agents (compact, interrupt, …).

### `Host`

Events emitted by the host process itself.

### `Lifecycle`

Agent lifecycle transitions (birth, death, update).

