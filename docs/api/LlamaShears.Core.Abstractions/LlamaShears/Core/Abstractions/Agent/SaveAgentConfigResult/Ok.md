# LlamaShears.Core.Abstractions.Agent.SaveAgentConfigResult.Ok

Assembly: `LlamaShears.Core.Abstractions`

Save succeeded; `NewHash` is the post-write digest.

## Methods

### `Ok`(string NewHash)

Save succeeded; `NewHash` is the post-write digest.

