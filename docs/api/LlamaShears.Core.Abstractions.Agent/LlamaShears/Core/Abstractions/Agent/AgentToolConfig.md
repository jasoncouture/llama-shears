# LlamaShears.Core.Abstractions.Agent.AgentToolConfig

Assembly: `LlamaShears.Core.Abstractions.Agent`

Per-agent tool-loop guardrails.

## Parameters

- `TurnLimit` — Maximum number of consecutive tool-call turns the loop will take before forcing the model to produce a user-facing answer.

## Properties

### `TurnLimit`

Maximum number of consecutive tool-call turns the loop will take before forcing the model to produce a user-facing answer.

## Methods

### `AgentToolConfig`(int TurnLimit)

Per-agent tool-loop guardrails.

#### Parameters

- `TurnLimit` — Maximum number of consecutive tool-call turns the loop will take before forcing the model to produce a user-facing answer.

