<!--
Keep this file empty (or with only comments) to skip heartbeat firings.

Add tasks below when you want the framework to wake you up periodically.
The contents of this file become a FrameworkUser turn delivered every
heartbeat interval; the framework prepends some system context.

You own this file. Delete it after consuming a one-shot request,
recreate it later, edit it freely. The throttle is the agent config's
HeartbeatPeriod — at most one firing per period, regardless of file
state.
-->
